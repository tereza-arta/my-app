name: Workflow for my-app

on:
  push:
    branches: [ main ]

jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Print message
        run: echo "Hello world!"
    

    
